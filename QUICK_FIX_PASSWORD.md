# Быстрое исправление ошибки: password authentication failed

## Причина

Ошибка возникает, когда volume БД уже существует с другим паролем. PostgreSQL инициализирует базу данных только при первом запуске на пустом volume. Если volume уже существует, переменные окружения `POSTGRES_PASSWORD` игнорируются.

## Решение (выберите один вариант)

### Вариант 1: Автоматическое исправление (рекомендуется) ⭐

```bash
# Удалите маркер инициализации
rm -f .db_initialized

# Запустите скрипт заново - он автоматически обнаружит проблему
./scripts/init.sh
```

При запросе выберите `yes` для удаления существующего volume БД.

### Вариант 2: Использование скрипта исправления

```bash
./scripts/fix-db-password.sh
```

Выберите вариант "Пересоздать БД" (`yes`).

### Вариант 3: Ручное исправление

```bash
# 1. Остановите все контейнеры
docker-compose down

# 2. Удалите volume БД (⚠️ удалит все данные!)
docker-compose down -v

# Или удалите конкретный volume:
docker volume ls | grep postgres_data
docker volume rm <имя_volume>

# 3. Запустите скрипт инициализации заново
./scripts/init.sh
```

## Проверка

После исправления проверьте:

```bash
# Проверка подключения к БД
docker-compose exec web python manage.py dbshell

# Или через psql
docker-compose exec db psql -U infralabs_user -d infralabs -c "SELECT version();"
```

Если команды выполняются без ошибок - проблема решена! ✅

## Предотвращение проблемы

Для предотвращения проблемы в будущем:

1. **Всегда используйте `./scripts/init.sh`** для первого запуска
2. **Не изменяйте `POSTGRES_PASSWORD` в `.env`** после первого запуска без пересоздания volume
3. **Если нужно изменить пароль:** используйте `./scripts/fix-db-password.sh`

📖 **Подробная документация:** [docs/FIX_PASSWORD_AUTH.md](docs/FIX_PASSWORD_AUTH.md)
