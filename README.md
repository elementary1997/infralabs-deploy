# Infra Labs - Минимальное развертывание

Репозиторий для быстрого развертывания Infra Labs с использованием готовых Docker образов.

## 📋 Требования

- Docker Engine 20.10+
- Docker Compose 2.0+
- Git

## 🚀 Быстрый старт

### Вариант 1: Автоматическая инициализация (рекомендуется)

Самый простой способ - использовать скрипт инициализации, который автоматически:
- Создает `.env` файл с настройками
- Генерирует `SECRET_KEY`
- Настраивает `ALLOWED_HOSTS`
- Настраивает пароли
- Запускает приложение с тестовыми данными

```bash
# 1. Клонирование репозитория
git clone https://github.com/elementary1997/infralabs-deploy.git
cd infralabs-deploy

# 2. Запуск скрипта инициализации
chmod +x scripts/init.sh
./scripts/init.sh
```

Скрипт запросит у вас:
- **ALLOWED_HOSTS** - домены/IP адреса (можно оставить пустым для localhost)
- **Пароль администратора** - пароль для `admin@infralabs.com` (по умолчанию: `admin123`)
- **Пароль PostgreSQL** - можно сгенерировать автоматически

После выполнения скрипта приложение будет полностью настроено и запущено!

### Вариант 2: Ручная настройка

Если вы предпочитаете ручную настройку:

```bash
# 1. Клонирование репозитория
git clone https://github.com/elementary1997/infralabs-deploy.git
cd infralabs-deploy

# 2. Создание .env файла
cp .env.example .env

# 3. Редактирование .env (обязательно!)
nano .env
```

**Обязательно измените в `.env`:**
- `DJANGO_SECRET_KEY` - сгенерируйте новый ключ
- `DJANGO_ALLOWED_HOSTS` - укажите ваш домен/IP через запятую (например: `yourdomain.com,192.168.1.100`)
- `POSTGRES_PASSWORD` - установите надежный пароль
- `ADMIN_PASSWORD` - пароль для администратора (по умолчанию: `admin123`)

**Пример настройки ALLOWED_HOSTS:**
```bash
# Для локального использования
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Для production с доменом
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Для нескольких серверов
DJANGO_ALLOWED_HOSTS=server1.example.com,server2.example.com,192.168.1.100
```

📖 **Подробная документация по настройке ALLOWED_HOSTS:** [docs/ALLOWED_HOSTS.md](docs/ALLOWED_HOSTS.md)

```bash
# 4. Запуск приложения
docker-compose up -d

# Или с указанием конкретной версии
VERSION=0.1.0 docker-compose up -d
```

### Доступ к приложению

После успешного запуска приложение будет доступно:
- **Frontend и API:** `http://your-server-ip` или `http://your-domain`
- **Django Admin:** `http://your-server-ip/admin/` или `http://your-domain/admin/`

**Учетные данные по умолчанию:**
- Email: `admin@infralabs.com`
- Password: `admin123` (или тот, который вы указали в `.env`)

⚠️ **ВАЖНО:** 
- Обязательно измените пароль администратора после первого входа!
- При первом запуске автоматически создается пользователь `admin@infralabs.com` и загружаются тестовые данные (курсы, модули, уроки, упражнения)

## 🔧 Переменные окружения

### Переменные Docker Compose

Можно задать при запуске через переменные окружения:

```bash
# Registry для образов
REGISTRY=docker.io/elementary1997

# Префикс образов
IMAGE_PREFIX=infralabs

# Версия образов (рекомендуется указывать конкретную версию)
VERSION=0.1.0

# Пароль PostgreSQL (можно также задать в .env через POSTGRES_PASSWORD)
POSTGRES_PASSWORD=your_secure_password
```

Пример запуска с указанием версии:
```bash
VERSION=0.1.0 POSTGRES_PASSWORD=secure_password123 docker-compose up -d
```

### Переменные в .env файле

Все переменные из `.env` файла автоматически загружаются в контейнеры `web`, `celery_worker` и `celery_beat`.

Основные переменные описаны в `.env.example`.

## 📦 Образы

Проект использует следующие Docker образы:

- `infralabs-db` - PostgreSQL база данных с предустановленными данными
- `infralabs-redis` - Redis кеш и очередь задач
- `infralabs-web` - Django веб-приложение
- `infralabs-celery_worker` - Celery worker для фоновых задач
- `infralabs-celery_beat` - Celery beat для периодических задач
- `infralabs-nginx` - Nginx веб-сервер с frontend

Все образы доступны в Docker Hub: `docker.io/elementary1997/infralabs-*`

## 🔄 Обновление приложения

Для обновления до новой версии:

```bash
# Указать новую версию
export VERSION=0.1.1

# Обновить образы
docker-compose pull

# Перезапустить с новыми образами
docker-compose up -d
```

## 💾 Управление данными

### Резервное копирование базы данных

```bash
# Создать бэкап
docker-compose exec db pg_dump -U infralabs_user infralabs > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из бэкапа
docker-compose exec -T db psql -U infralabs_user infralabs < backup_20250110_120000.sql
```

### Восстановление полной базы данных из основного проекта

Для восстановления полной базы данных (со всеми пользователями, прогрессом, курсами и т.д.) из основного проекта:

**1. На основном проекте - экспортируйте базу данных:**

```bash
# Используйте стандартный pg_dump или скрипт из основного проекта
docker exec infralabs_db pg_dump \
    -U infralabs_user \
    -d infralabs \
    --clean \
    --if-exists \
    --create \
    --format=plain \
    --no-owner \
    --no-privileges \
    > infralabs_full_db_$(date +%Y%m%d_%H%M%S).sql
```

**2. Скопируйте файл на сервер с infralabs-deploy:**
```bash
scp infralabs_full_db_*.sql user@new-server:/path/to/infralabs-deploy/backups/
```

**3. На сервере с infralabs-deploy - импортируйте базу данных:**
```bash
# Убедитесь, что контейнер БД запущен
docker-compose up -d db

# Импортируйте базу данных (⚠️ ВНИМАНИЕ: заменит всю существующую БД!)
./scripts/import-full-db.sh ./backups/infralabs_full_db_YYYYMMDD_HHMMSS.sql
```

⚠️ **ВАЖНО**: Полное восстановление **ЗАМЕНИТ** всю существующую базу данных, включая всех пользователей и все данные!

📖 Подробная документация: [docs/DATABASE_RESTORE.md](docs/DATABASE_RESTORE.md)

### Удаление всех данных (⚠️ ОСТОРОЖНО!)

```bash
# Остановить контейнеры
docker-compose down

# Удалить volumes (удалит все данные!)
docker-compose down -v
```

## 🐛 Troubleshooting

### Контейнеры не запускаются

```bash
# Проверить логи
docker-compose logs

# Проверить статус
docker-compose ps

# Перезапустить все сервисы
docker-compose restart
```

### База данных не доступна

```bash
# Проверить статус базы данных
docker-compose exec db pg_isready -U infralabs_user

# Проверить логи базы данных
docker-compose logs db
```

### Nginx возвращает 502 Bad Gateway

```bash
# Проверить, что web контейнер запущен
docker-compose ps web

# Проверить логи web сервиса
docker-compose logs web

# Проверить, что миграции применены
docker-compose exec web python manage.py migrate
```

### Иконки/файлы не загружаются

```bash
# Проверить, что media volume смонтирован
docker-compose exec web ls -la /app/media

# Проверить права доступа
docker-compose exec web chmod -R 755 /app/media
```

### Sandbox контейнеры не создаются

```bash
# Проверить доступ к Docker socket
docker-compose exec web ls -la /var/run/docker.sock

# Проверить, что пользователь в группе docker
docker-compose exec web id
```

## 📝 Структура проекта

```
.
├── docker-compose.yml         # Основной файл Docker Compose
├── .env.example               # Пример файла с переменными окружения
├── .env                       # Ваш файл с настройками (создается вручную)
├── .gitignore                 # Игнорируемые файлы
├── scripts/                   # Скрипты для управления
│   ├── init.sh               # Скрипт автоматической инициализации (первый запуск)
│   ├── export-full-db.sh     # Экспорт полной БД (для использования в основном проекте)
│   ├── import-full-db.sh     # Импорт полной БД в infralabs-deploy
│   └── create_admin.py       # Скрипт для создания/обновления администратора
├── backups/                   # Директория для файлов дампов БД (создается автоматически)
├── docs/                      # Документация
│   ├── DATABASE_RESTORE.md   # Подробная документация по восстановлению БД
│   └── ALLOWED_HOSTS.md      # Документация по настройке ALLOWED_HOSTS
├── .env.example              # Пример файла с переменными окружения
└── README.md                  # Этот файл
```

## 🔒 Безопасность

**Обязательно выполните перед развертыванием в production:**

1. Измените `SECRET_KEY` в `.env`
2. Измените `POSTGRES_PASSWORD` в `.env` или через переменную окружения
3. Измените пароль администратора после первого входа
4. Настройте `ALLOWED_HOSTS` для вашего домена
5. Установите `DEBUG=False` в production
6. Настройте SSL/TLS сертификат (рекомендуется использовать Nginx reverse proxy)
7. Ограничьте доступ к портам 8000, 5432, 6379 (только nginx должен быть публичным)

## 📞 Поддержка

Если у вас возникли проблемы:

1. Проверьте раздел Troubleshooting
2. Проверьте логи: `docker-compose logs -f`
3. Убедитесь, что все переменные окружения настроены правильно
4. Проверьте, что используете актуальную версию образов

## 📄 Лицензия

См. основной репозиторий проекта.
