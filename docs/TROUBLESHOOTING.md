# Устранение неполадок

## Проблема: password authentication failed for user "infralabs_user"

**Симптомы:**
- Ошибка `OperationalError: password authentication failed for user "infralabs_user"`
- Django не может подключиться к PostgreSQL
- Админка не открывается, показывает ошибку аутентификации БД

**Причина:**
Пароль PostgreSQL в `.env` не совпадает с паролем, использованным при создании volume БД. PostgreSQL хранит пароль при инициализации, и просто изменение переменной окружения не помогает, если volume уже существует.

**Решение:**

### Быстрое исправление (рекомендуется):

```bash
./scripts/fix-db-password.sh
```

Этот скрипт предложит два варианта:
1. **Пересоздать БД с нуля** - удалит все данные, но гарантированно решит проблему (для первого запуска)
2. **Изменить пароль в существующей БД** - сохранит данные, но требует знать старый пароль

### Ручное исправление (для первого запуска):

```bash
# 1. Остановите контейнеры
docker-compose down

# 2. Удалите volume БД (⚠️ удалит все данные!)
docker-compose down -v
# Или конкретный volume:
docker volume rm $(docker volume ls --format "{{.Name}}" | grep postgres_data)

# 3. Убедитесь, что пароль в .env правильный
grep POSTGRES_PASSWORD .env

# 4. Обновите DATABASE_URL с правильным паролем
nano .env
# Установите: DATABASE_URL=postgresql://infralabs_user:ВАШ_ПАРОЛЬ@db:5432/infralabs

# 5. Запустите заново
export POSTGRES_PASSWORD=$(grep "^POSTGRES_PASSWORD=" .env | cut -d '=' -f2)
docker-compose up -d
```

### Если нужно сохранить данные:

Если volume уже содержит важные данные, измените пароль в существующей БД:

```bash
# 1. Запустите БД со старым паролем (временно установите в .env)
nano .env  # POSTGRES_PASSWORD=старый_пароль
docker-compose up -d db

# 2. Подключитесь и измените пароль
docker-compose exec db psql -U infralabs_user -d postgres -c "ALTER USER infralabs_user WITH PASSWORD 'новый_пароль';"

# 3. Обновите .env с новым паролем
nano .env  # POSTGRES_PASSWORD=новый_пароль
# И DATABASE_URL=postgresql://infralabs_user:новый_пароль@db:5432/infralabs

# 4. Перезапустите все сервисы
docker-compose restart
```

📖 **Подробная документация:** [FIX_PASSWORD_AUTH.md](FIX_PASSWORD_AUTH.md)

## Проблема: Админка Django редиректит на HTTPS

**Симптомы:**
- При попытке открыть админку Django (`/admin/`) происходит редирект на HTTPS
- Браузер показывает ошибку "ERR_SSL_PROTOCOL_ERROR" или "This site can't be reached"
- В логах нет ошибок, но страница не открывается

**Причина:**
Django настроен на принудительный редирект HTTP → HTTPS через `SECURE_SSL_REDIRECT = True`, но SSL сертификат не настроен.

**Решение:**

### Вариант 1: Для локального развертывания (рекомендуется)

Установите `DJANGO_DEBUG=True` в файле `.env`:

```bash
DJANGO_DEBUG=True
```

Это отключит принудительный HTTPS редирект для локальной разработки.

### Вариант 2: Для production с SSL

Если у вас настроен SSL (через reverse proxy или напрямую), убедитесь что:

1. `DJANGO_DEBUG=False` в `.env`
2. `SECURE_SSL_REDIRECT` остается включенным (по умолчанию при `DEBUG=False`)
3. Nginx или другой reverse proxy настроен на обработку SSL

### Вариант 3: Временное отключение для тестирования

Если нужно временно отключить редирект без изменения DEBUG:

Добавьте в `.env` (если поддерживается в вашей версии):
```bash
DJANGO_SECURE_SSL_REDIRECT=False
```

**Примечание:** Эта переменная может не работать, если образ уже собран с жестко заданным `SECURE_SSL_REDIRECT`. В этом случае используйте Вариант 1.

## Проблема: Ошибка при создании администратора

**Симптомы:**
- В логах контейнера `web` видна ошибка при создании администратора
- Администратор не создается автоматически

**Решение:**

1. Проверьте логи:
```bash
docker-compose logs web | grep -i admin
```

2. Убедитесь, что переменные окружения установлены в `.env`:
```bash
ADMIN_EMAIL=admin@infralabs.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_password
```

3. Создайте администратора вручную:
```bash
docker-compose exec web python manage.py createsuperuser
```

## Проблема: База данных не подключается

**Симптомы:**
- Контейнер `web` не запускается
- Ошибки в логах: "could not connect to server"

**Решение:**

1. Проверьте, что контейнер БД запущен:
```bash
docker-compose ps db
```

2. Проверьте логи БД:
```bash
docker-compose logs db
```

3. Убедитесь, что пароль в `.env` совпадает с паролем в `docker-compose.yml`:
```bash
# В .env
POSTGRES_PASSWORD=your_password

# В docker-compose.yml (должен совпадать)
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-infralabs_pass_change_in_prod}
```

4. Перезапустите контейнеры:
```bash
docker-compose down
docker-compose up -d
```

## Проблема: Статические файлы не загружаются

**Симптомы:**
- CSS/JS файлы не загружаются (404 ошибки)
- Админка Django выглядит без стилей

**Решение:**

1. Проверьте, что статические файлы собраны:
```bash
docker-compose exec web python manage.py collectstatic --noinput
```

2. Проверьте, что volume `static_volume` смонтирован:
```bash
docker-compose exec web ls -la /app/staticfiles
```

3. Проверьте права доступа:
```bash
docker-compose exec web chmod -R 755 /app/staticfiles
```

4. Перезапустите nginx:
```bash
docker-compose restart nginx
```

## Проблема: ALLOWED_HOSTS ошибка

**Симптомы:**
- Ошибка "DisallowedHost" в логах
- Страница не открывается

**Решение:**

1. Добавьте ваш домен/IP в `.env`:
```bash
DJANGO_ALLOWED_HOSTS=your-domain.com,192.168.1.100,localhost
```

2. Перезапустите web контейнер:
```bash
docker-compose restart web
```

3. Проверьте текущие настройки:
```bash
docker-compose exec web python manage.py shell -c "from django.conf import settings; print(settings.ALLOWED_HOSTS)"
```

## Проблема: Контейнеры не запускаются

**Симптомы:**
- `docker-compose up -d` завершается с ошибкой
- Контейнеры сразу останавливаются

**Решение:**

1. Проверьте логи всех сервисов:
```bash
docker-compose logs
```

2. Проверьте статус контейнеров:
```bash
docker-compose ps -a
```

3. Проверьте синтаксис `docker-compose.yml`:
```bash
docker-compose config
```

4. Проверьте доступность образов:
```bash
docker-compose pull
```

## Проблема: Порт уже занят

**Симптомы:**
- Ошибка "port is already allocated"
- Контейнер не может запуститься

**Решение:**

1. Найдите процесс, использующий порт:
```bash
# Для порта 80
sudo lsof -i :80

# Для порта 8000
sudo lsof -i :8000
```

2. Остановите процесс или измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8080:80"  # Вместо 80:80
```

## Проблема: Медленная работа приложения

**Симптомы:**
- Страницы загружаются медленно
- Таймауты при запросах

**Решение:**

1. Проверьте использование ресурсов:
```bash
docker stats
```

2. Увеличьте количество workers в `docker-compose.yml`:
```yaml
command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 8
```

3. Проверьте логи на наличие ошибок:
```bash
docker-compose logs web | tail -50
```

## Получение помощи

Если проблема не решена:

1. Соберите информацию:
```bash
# Логи всех сервисов
docker-compose logs > logs.txt

# Статус контейнеров
docker-compose ps > status.txt

# Конфигурация
docker-compose config > config.txt
```

2. Проверьте документацию:
   - [README.md](../README.md)
   - [QUICKSTART.md](../QUICKSTART.md)
   - [ALLOWED_HOSTS.md](ALLOWED_HOSTS.md)

3. Создайте issue в репозитории с собранной информацией
